# Google OAuth Client ID (тип "Web application") для Google Drive-синхронізації.
# Скопіюй у .env.local (уже покривається *.local у .gitignore) і встав свій.
# Порожньо — застосунок працює локально, без синхронізації.
VITE_GOOGLE_CLIENT_ID=
