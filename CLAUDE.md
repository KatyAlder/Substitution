# Заміни — застосунок для пошуку замін учителів

Повна специфікація й домовленості раніше жили в `zaminy-tz.md` — сесія 9 прибрала цей
файл із репозиторію й усієї git-історії (репозиторій публічний, файл ніс забагато
приватної інформації про школу). Джерело істини про рішення й домовленості тепер —
тільки цей файл, розділи "Стан проєкту після сесії N" і "Рішення, уточнені під час
сесії N" нижче.

## Стек

Vite + React + TypeScript, звичайний CSS (mobile-first, без UI-фреймворків), Vitest.
Стан — localStorage як основне джерело, з опційною фоновою синхронізацією через
Google Drive (один JSON-файл у власному Drive користувача, `drive.file`-скоуп,
без Client ID — застосунок працює локально, як і раніше). Деталі — сесія 8 нижче.

## Команди

- `npm run dev` — дев-сервер (`server.host: true` у vite.config.ts, слухає всі інтерфейси —
  так виправили баг, коли осиротілий процес слухав лише IPv6 і сторінка була порожня)
- `npm test` — юніт-тести ранжування
- `npx tsc -b` — перевірка типів

## Стан проєкту після сесії 1 (2026-08-07)

Зроблено (розділ 10 ТЗ, крок 1 і частина кроку 2): модель даних (розділ 3), логіка
ранжування (розділ 4) з юніт-тестами на всі узгоджені крайові випадки, вигадані сідові
дані, локальне збереження стану, імпорт JSON (розділ 8, часткове оновлення), один екран —
вибір відкритої заміни → ранжований список кандидатів по тирах з повною карткою.

## Стан проєкту після сесії 2 (2026-08-07)

Крок 2 "Ядро" з розділу 10 ТЗ тепер закритий повністю:

- **Активні заміни стали інтерактивними** — кожна картка кандидата має кнопки
  `WhatsApp` / `Погодився` / `Відмовився` / `Не відповів` (`src/components/CandidateCard.tsx`,
  `src/data/actions.ts`). Згода одразу закриває заміну й перемикає чергу на наступну
  відкриту; відмова/мовчання лишають заміну відкритою й підвищують лічильники живо
  (бонус "3 відмови поспіль" спрацьовує вже під час поточного опитування).
- **Екран профілів** (`src/screens/ProfileScreen.tsx`, `src/profile/stats.ts`) — навантаження
  по класах/предметах, присутність і золоті години по днях тижня, телефон, бейджі
  погодинника/куратора, статистика за поточний календарний місяць.
- **Завчасні заміни працюють по-справжньому** (`src/components/BroadcastPanel.tsx`) —
  перш ніж потрапити в чергу опитування, вони чекають у панелі "розсилки" одним спільним
  повідомленням-списком; після "Позначити всі як 'в чаті'" з'являються в черзі зі
  статусом `in-chat`, і згода на них автоматично пише `closedVia: "voluntary"`.
- Проста навігація-вкладки в `App.tsx` між "Активні заміни" і "Профілі".

Свідомо не робили (крок 3 "Решта" з розділу 10): календар, окрема вкладка статистики
(зведення по всіх учителях, на відміну від картки одного вчителя на профілі), парсер
тексту (розділ 6), екран для запуску імпорту JSON (сама функція `importSchedule.ts` вже
є, кнопки в застосунку — нема), GitHub-синхронізація й авторизація через Google.

**Відомий відкритий пункт розділу 9** — авдиторії насправді дві (Hardlab і Softlab) зі
своїми переліками предметів, а не одна, як зараз у `TECHNICAL_SUBJECT_ROOMS`
(`src/config/settings.ts`). Kate свідомо відклала виправлення до реальних вересневих
даних — не чіпати це передчасно.

## Стан проєкту після сесії 3 (2026-08-08)

З кроку 3 "Решта" з розділу 10 ТЗ зроблено перший шматок — екран "Календар":

- **`effectiveDaySchedule`** (`src/calendar/effectiveDay.ts`) — функція "актуальний
  розклад слоту = база + заміни" (розділ 2 ТЗ), якої раніше ніде в коді не було.
  Закрита заміна знімає слот з календаря відсутнього і додає заміснику з підписом
  "заміна за {ім'я}"; відкрита/`in-chat`/тупик лишає слот за відсутнім із бейджем
  статусу пошуку. Юніт-тести на всі крайові випадки — `src/calendar/effectiveDay.test.ts`.
- **`CalendarScreen.tsx`** — список уроків на день (не тижнева сітка), обов'язковий
  вибір фільтра вчитель/клас/авдиторія перед показом розкладу, навігація датою.
  Для режиму "вчитель" додатково показуються блоки присутності на цей день тижня.
  Нові перевикористовувані компоненти: `ChipPicker` (узагальнений рядковий пікер —
  перемикач режиму, списки класів/авдиторій), `DayAgenda` (рендер розкладу дня).
- Вкладка "Календар" в `App.tsx` між "Активні заміни" і "Профілі" (порядок за
  розділом 5 ТЗ).

Свідомо не робили (лишається з кроку 3): окрема вкладка статистики (зведення по всіх
учителях), парсер тексту (розділ 6), екран для запуску імпорту JSON, GitHub-синхронізація
й авторизація через Google. Відкритий пункт розділу 9 (Hardlab/Softlab) — той самий, не
чіпали.

## Стан проєкту після сесії 4 (2026-08-08)

Ще один шматок кроку 3 "Решта" — вкладка "Статистика":

- **`teacherMonthlyRows` / `deadEndsForMonth` / `shiftMonth`**
  (`src/stats/schoolStats.ts`) — зведення по всіх учителях за місяць. Рахунок
  по кожному вчителю перевикористовує вже готовий `monthlyStats`
  (`src/profile/stats.ts`, з профілю) у циклі, нової логіки підрахунку не
  писали. Тупики — окремий список (`deadEndsForMonth`), бо не прив'язані до
  конкретного вчителя. Юніт-тести — `src/stats/schoolStats.test.ts`.
- **`StatisticsScreen.tsx`** — перемикач місяця ◀/▶ (дефолт — поточний),
  таблиця "вчитель / замін / відмов / мовчання" по всіх учителях (включно з
  нульовими рядками), список тупиків за місяць з деталями (дата, клас, урок,
  хто був відсутній).
- Вкладка "Статистика" в `App.tsx` останньою (порядок розділу 5 ТЗ: Активні
  заміни → Календар → Профілі → Статистика).

Кроку 3 "Решта" лишилось: парсер тексту (розділ 6), екран для запуску імпорту
JSON, GitHub-синхронізація й авторизація через Google. Відкритий пункт
розділу 9 (Hardlab/Softlab) — той самий, не чіпали.

## Стан проєкту після сесії 5 (2026-08-08)

Два окремі коміти: спершу багфікси, потім парсер тексту.

**Багфікси** (знайдено й виправлено дорогою, не частина запланованої фічі):
- `toISOString().slice(0, 10)` конвертує в UTC, а не бере локальну дату — у
  поясі UTC+3 це відкушувало день назад біля півночі. Ламало стрілки дати в
  `CalendarScreen.tsx` і `currentMonth()` у `profile/stats.ts`. Виправлено
  спільними `todayIso()`/`addDays()` у `src/ranking/presence.ts` (рахують
  локальну дату напряму через `getFullYear`/`getMonth`/`getDate`) — інші
  місця, що рахують дату, мають брати звідти, а не заново писати
  `toISOString`.
- У `profile/stats.ts` (`classLoad`, з сесії 1-2) у рядку-ключі Map замість
  пробілу був буквальний нульовий байт — не ламало функціонально (ключ і так
  лишався унікальним), але робило файл "бінарним" для git-діффа. Замінено на
  пробіл.

**Парсер тексту запиту на заміну** (розділ 6 ТЗ) — останній нетривіальний
шматок кроку 3 "Решта":
- **`src/parser/parseRequest.ts`** — чистий розбір повідомлення без звернення
  до стану: прізвище (двобічний префікс-збіг зі словами `teacher.name`, токени
  коротші 3 символів ігноруються), дата (сьогодні/завтра/день тижня/"число
  місяця", пріоритет у цьому порядку), "на весь день", час ЧЧ:ММ, номер уроку.
- **`src/parser/resolveRequest.ts`** — резолюція проти `AppState`: уроки
  вчителя в конкретний день, підбір слоту за номером уроку чи часом, найближчий
  дзвінок школи, якщо час поза всіма слотами, перевірка конфлікту з наявною
  заміною на той самий слот. Нічого не вгадує мовчки — усе, що не резолвилось
  однозначно, повертається як дані для форми, а не як рішення.
- **`createSubstitutions`** (`src/data/actions.ts`) — нова дія, якої раніше не
  було: заводить одну чи кілька замін одразу зі статусом `open`; далі в чергу
  чи в розсилку розводить наявна логіка `CandidatesScreen` за `mode`/`status`.
- **`ParseRequestPanel.tsx`** (згорнута панель у "Активних замінах", над
  чергою) — вчитель/дата/урок/режим завжди редаговані тапом; неоднозначне ім'я
  чи слот показує пікер, а не здогадку; конфлікт слоту блокує кнопку
  створення з поясненням; "на весь день" — чекбокс-список уроків дня
  (`LessonChecklist.tsx`) із живим прев'ю тексту для чату
  (`buildWholeDayMessage` у `src/whatsapp.ts`).

## Стан проєкту після сесії 6 (2026-08-08)

Відхилення від розділу 10 ТЗ на прохання Kate — не крок 3 "Решта", а власна
дизайн-система на основі матеріалів поза застосунком:

- **Дизайн-мокап** на основі шрифту (Cambria) і кольорів (teal/purple/bronze +
  фонові тони) з навчальних матеріалів Kate — спершу окремий Artifact-прев'ю з
  реальними сідовими даними (Ткаченко Ігор, Гончарук Ірина, ...), затверджений
  без правок.
- **Перенесено в `src/index.css`** — шрифт Cambria/Georgia скрізь без винятків;
  курсив для другорядного тексту (dt-мітки, підказки, підписи), small-caps для
  вкладок навігації й заголовків тирів — заміна ієрархії другою гарнітурою,
  якої Kate не хотіла ("шрифт скрізь, без винятків").
- Шість тирів (`src/ranking/rank.ts`, `Tier = 1..6`) згруповано в три кольорові
  смуги за парами пріоритету: 1-2 → teal-бокс, 3-4 → purple-бокс, 5-6 →
  bronze-бокс (`TierSection.tsx`, клас `tier-section--{1|2|3}`) — у
  затвердженому макеті випадково зібрались лише тири 1-3, тож розподіл на
  шість довелось вирішувати самостійно під час перенесення в код.
- Бейдж "золота година" (`CandidateCard.tsx`) тепер видно одразу біля імені,
  не тільки в рядку статистики картки.
- Семантичні кольори (згода/відмова/бонус) свідомо лишені окремо від
  бренд-трійки teal/purple/bronze — інакше кнопки "Погодився"/"Відмовився"
  втратили б миттєву впізнаваність під час обдзвону.
- Темна тема виведена з тих самих відтінків (не інвертована навмання) —
  застосунок і так підтримує dark mode з сесії 1.
- `.claude/launch.json`: додано `autoPort: true` — порт 5173 конфліктував з
  дев-сервером в іншому чаті під час перевірки; на застосунок не впливає
  (немає OAuth/вебхуків, прив'язаних до конкретного порту).

Розділу 10 ТЗ це не стосується — крок 3 "Решта" й далі чекає на два пункти
нижче.

## Стан проєкту після сесії 7 (2026-08-08)

Останній нерозпочатий пункт кроку 3 "Решта", крім GitHub-синхронізації:

- **Екран запуску імпорту JSON** (розділ 8 ТЗ) — сама функція
  `importSchedule.ts` (злиття) уже існувала з сесії 1, бракувало UI. Додано:
  - **`src/data/importValidation.ts`** — чиста структурна валідація JSON
    перед злиттям (`parseScheduleImport`): кожне поле перевіряється окремо,
    перша ж розбіжність повертає помилку з точним шляхом (наприклад
    `teachers[0].subjects: очікувався масив рядків`), часткового імпорту при
    помилці нема. Друга функція, `summarizeImport`, рахує прев'ю "що
    зміниться" (нові/оновлені вчителі, нові/оновлені записи розкладу) і
    окремо визначає `teacherId` у `schedule`, якого нема ні в базі, ні серед
    імпортованих вчителів — попередження про можливий одрук, не блокує
    імпорт. Юніт-тести — `src/data/importValidation.test.ts`.
  - **`ImportScreen.tsx`** — textarea для вставки JSON, живий прев'ю змін під
    час набору, `window.confirm` із коротким підсумком перед самим записом
    (той самий патерн, що й "Тупик" у `CandidatesScreen`), повідомлення про
    успіх з `updatedAt` імпортованої бази.
  - Нова вкладка "Імпорт" — **п'ята, останньою** в `App.tsx`, після
    Статистики. Розділ 5 ТЗ фіксує порядок лише перших чотирьох вкладок,
    імпорт як одноразова процедура на семестр у цей список не входив.
  - `scheduleKey` в `importSchedule.ts` став експортованим — переюзаний у
    `summarizeImport` для підрахунку нових/оновлених записів розкладу тим
    самим ключем `(teacherId, weekday, lesson)`, що й саме злиття.
  - Перевірено в браузері: коректний JSON показує правильний прев'ю
    (включно з попередженням про одрук в id), імпорт реально пише в
    localStorage, textarea очищається після успіху, невалідний JSON блокує
    кнопку "Імпортувати" й показує пояснення.

## Стан проєкту після сесії 8 (2026-08-08)

Останній пункт кроку 3 "Решта" з розділу 10 ТЗ — синхронізація. Kate надала
робочий референс (код іншого свого застосунку, "Стайня"), де вже реалізовано
й перевірено на практиці вхід і збереження через Google. Розбір коду показав
розбіжність із формулюванням у ТЗ/цьому файлі: це не GitHub, а **Google
Drive** — Kate підтвердила, що приймаємо Drive як бекенд, і що синхронізація
потрібна для одного акаунта між кількома власними пристроями Kate, не для
спільного доступу різних людей. План погоджено через `ExitPlanMode` перед
написанням коду.

- **`src/sync/googleAuth.ts`** — вхід через Google Identity Services
  (`google.accounts.oauth2.initTokenClient`), скоуп `drive.file` (доступ лише
  до файлу, який сам створив застосунок, не до всього Drive і не до профілю
  користувача — це не логін-ідентифікація, а видача токена на файл). GIS-скрипт
  довантажується в рантаймі лише якщо задано `VITE_GOOGLE_CLIENT_ID`, а не
  завжди, як у Стайні.
- **`src/sync/googleDrive.ts`** — `findStateFile/createStateFile/loadStateFile/
  getFileMeta/uploadStateFile`, перенесені зі Стайні майже дослівно (ті самі
  REST-ендпоінти Drive v3, multipart-створення файлу).
- **`src/sync/pickNewer.ts`** (+ тести) — свідомо простіша стратегія, ніж
  по-записове злиття зі Стайні (`mergeData`/tombstone-мапа видалень): весь
  `AppState` цілком, виграє той, у кого свіжіша мітка `meta.updatedAt`. Це
  вирішує конфлікти між різними редакторами одночасно — сценарій, який Kate
  виключила (один акаунт, кілька своїх пристроїв). Якщо колись знадобиться
  тонше злиття — окрема наступна ітерація, не будували наперед.
- **`AppState.meta?.updatedAt`** (`src/types/state.ts`) — нове технічне поле,
  навмисно окреме від наявного `AppState.updatedAt` (це дата збірки бази з
  імпорту, розділ 8 ТЗ, використовується в `ImportScreen`/`importValidation` —
  переплутати ці два поля означало б зламати показ дати імпорту).
  Проставляється в `useAppState.ts` при кожній зміні стану (обгортка над
  `setState`, не в кожній дії з `actions.ts` — дії лишились чистими).
- **`src/data/AppStateContext.tsx`** — виявилось, що `useAppState()` раніше
  викликався окремо в кожному з 5 екранів (`CandidatesScreen`, `CalendarScreen`,
  `ProfileScreen`, `StatisticsScreen`, `ImportScreen`) як 5 незалежних
  локальних станів, які "збігались" лише тому, що екрани ніколи не
  монтувалися одночасно (проста умовна навігація в `App.tsx`, без роутера).
  Для єдиного місця, що веде синхронізацію й бейдж статусу, це довелось
  підняти в один спільний контекст — `AppStateProvider` (обгортає `<App />`
  у `main.tsx`) плюс `useAppState()`/`useSyncStatus()` з того самого файлу.
  Екрани змінили лише шлях імпорту (`../data/useAppState` →
  `../data/AppStateContext`), сама точка виклику й деструктуризація —
  без змін.
- **`src/sync/useDriveSync.ts`** — дебаунс-запис у Drive (1.5с тиші),
  перед кожним записом звіряє `modifiedTime` файлу з останнім відомим; якщо
  змінився з іншого пристрою — підвантажує, застосовує `pickNewer`, показує
  повідомлення "Дані оновились з іншого пристрою" замість мовчазного
  перезапису. Retry за 10с при мережевій помилці.
- **`src/sync/SyncBar.tsx`** — тонкий рядок над вкладками (видно з будь-якої
  вкладки, `App.tsx`): крапка-бейдж локально/зберігаю/збережено/помилка,
  кнопка "Увійти через Google"/"Вийти". Без `VITE_GOOGLE_CLIENT_ID` не
  рендериться зовсім — застосунок працює так само, як до цієї сесії.

**Не закрито до кінця — потребує дій від Kate поза кодом:**
- Створити власний OAuth Client ID у Google Cloud Console (окремий від
  Стайні) і покласти в `.env.local` (див. `.env.example`) — це дія логіну
  й керування Google-акаунтом, її не роблю я.
- Поки триває робота над синхронізацією, порт дев-сервера варто тимчасово
  зафіксувати (`.claude/launch.json`, `autoPort: false` чи явний порт) —
  Google звіряє точний origin `http://localhost:PORT` зі списком дозволених,
  а `autoPort: true` (сесія 6) міняє порт між запусками.
- Сам клік "Увійти через Google" і вибір акаунта в Google-вікні — дія
  логіну, яку має виконати Kate особисто; я довів код до цього кроку й
  перевірив типи (`npx tsc -b`) та юніт-тести (`npm test`, 89/89 зелені,
  включно з новими `pickNewer.test.ts`), але не сам вхід.
- Живу перевірку в браузері (Browser pane) в цій сесії заблокував конфлікт
  портів з дев-сервером іншого чату на 5173 (те саме обмеження, що спричинило
  `autoPort` в сесії 6) — не дефект коду, sync-бейдж за конструкцією не
  рендериться без Client ID, тож поведінка застосунку без нього не змінилась.

## Стан проєкту після сесії 9 (2026-08-08)

Завершено те, що лишалось поза кодом із сесії 8 — розділ 10 ТЗ тепер
закритий повністю, і з коду, і з налаштувань акаунта:

- Kate створила окремий OAuth Client ID (тип "Web application") у власному
  проєкті Google Cloud Console, з `http://localhost:5173` в Authorized
  JavaScript origins — я провів її по кроках консолі, сам ID вона вставила в
  чат (публічний ідентифікатор клієнта, не секрет, тож нормально передати
  текстом і записати в `.env.local`).
- `.claude/launch.json`: `autoPort` повернуто на `false` (був `true` із
  сесії 6) — Google звіряє точний origin з портом, порт мусить бути сталим,
  поки синхронізація в роботі.
- Перша спроба входу дала статус "помилка" в `SyncBar` — причина
  виявилась не в коді: Google Drive API був не увімкнений у щойно
  створеному проєкті Cloud Console (окремий крок від створення Client ID,
  який не був явним пунктом у моїй інструкції сесії 8 — додати в майбутні
  інструкції з нуля). Після `APIs & Services → Library → Google Drive API →
  Enable` вхід і запис пройшли одразу, бейдж синхронізації став
  "збережено".
- Спроба відтворити вхід у Browser pane для діагностики показала окреме,
  не пов'язане з цим обмеження: вбудований прев'ю-браузер блокує спливне
  вікно Google (`Failed to open popup window`) — сама діагностика помилки
  (Enabled APIs) пройшла через справжній браузер Kate й читання її
  DevTools/опис на слово, Browser pane для цього конкретного флоу не
  придатний.

Розділ 10 ТЗ формально закритий повністю. Далі лишається тільки відкритий
пункт розділу 9 нижче й, можливо, доробки за реальними вересневими даними,
коли з'явиться усталений розклад (реалізацію було заплановано на вересень
2026, після появи усталеного розкладу).

**Відомий відкритий пункт розділу 9** (не чіпати передчасно) — авдиторії
насправді дві (Hardlab і Softlab) зі своїми переліками предметів, а не одна,
як зараз у `TECHNICAL_SUBJECT_ROOMS` (`src/config/settings.ts`). Kate свідомо
відклала це до реальних вересневих даних.

## Стан проєкту після сесії 10 (2026-08-08)

Три окремі теми: візуальний баг, деплой на GitHub Pages, приватність сідових
даних.

**Баг "перемикання замін не працює"** — виявився не логічною, а візуальною
пасткою: коли в черзі рівно одна заміна, `.slot-summary` (деталі вибраної
заміни під чергою, `CandidatesScreen.tsx`) мала майже той самий стиль
(рамка/фон/заокруглення), що й `.slot-picker__item`, і читалась як другий
клікабельний пункт списку. Фікс — прибрати рамку й фон у `.slot-summary`
(`src/index.css`), логіку кліку не чіпали, вона й так була справною
(підтверджено прямим DOM-кліком і реальним відтворенням із кількома
замінами в черзі).

**Деплой на GitHub Pages** — Kate показала свій попередній застосунок
("Стайня"), який відкривається з телефону й ПК незалежно; з'ясувалось, що
причина не в іншій логіці входу (код входу тут і так перенесений зі Стайні
майже дослівно, сесія 8), а в тому, що Google OAuth прив'язаний до
конкретного origin — `localhost:5173` працює лише на самому ПК:
- `vite.config.ts`: `base` тепер залежить від команди (`/Substitution/` для
  `build`, `/` для `dev`) — GitHub Pages сервірує з підпапки за назвою
  репозиторію, дев-сервер лишається на корені.
- `.github/workflows/deploy.yml` — авто-збірка й деплой при пуші в master
  (`npm ci` → `npm test` → `npm run build` → `actions/deploy-pages`),
  `VITE_GOOGLE_CLIENT_ID` іде через repository secret з тим самим ім'ям
  (типова помилка першого разу — Kate назвала секрет інакше, workflow
  збудувався, але без Client ID; перейменування виправило).
- Репозиторій `github.com/KatyAlder/Substitution` — новий, спершу приватний
  за перевагою Kate, але GitHub Pages на безкоштовному плані працює тільки з
  публічними репозиторіями (приватні потребують GitHub Pro) — Kate обрала
  зробити репозиторій публічним, а не платити. Реальні дані школи (імена,
  телефони, розклад) у код і git ніколи не потрапляють — вони живуть тільки
  в особистому Google Drive Kate через `drive.file`-скоуп, тож публічність
  репозиторію на них не впливає.
- `Settings → Pages → Source` треба вручну перевести на "GitHub Actions"
  (не автоматично, окрема дія в UI, я не можу зробити її за Kate) — без
  цього перші два запуски workflow падали на кроці деплою.
- Робочий URL: `https://katyalder.github.io/Substitution/` — Google Cloud
  Console Authorized JavaScript origins доповнено цим origin (без шляху,
  origin — тільки схема+хост).

**Приватність сідових даних** — два незалежні запити Kate одразу після
деплою, обидва через ризик, що вигадані дані тепер публічні:
- `src/data/seed.ts`: прибрано поле `phone` з усіх 12 учителів (сам ризик —
  випадковий збіг вигаданого номера з реальним). `Teacher.phone` лишився
  опційним полем типу — реальний імпорт JSON і далі підставляє справжні
  номери, кнопка WhatsApp без номера й так коректно показувала "немає
  номера телефону" (поведінка вже існувала, `src/whatsapp.ts`).
- Павлюк Олена перейменована на Клименко Оксану — та сама роль у сідових
  даних (кураторка 7-А, приклад завчасної заміни розділу 8 ТЗ, історична
  заміна для лічильника "замін за місяць"), тільки інше ім'я/id. Довелось
  синхронно поправити `rank.test.ts` і `stats.test.ts`, які напряму
  посилались на `pavliuk-olena` за id.
- Kate окремо видалила `zaminy-tz.md` через GitHub UI ("забагато приватної
  інфи") — з'ясувалось і пояснено Kate, що видалення файлу новим комітом
  не прибирає його зі старих комітів, а репозиторій уже публічний. З її
  явного підтвердження всю git-історію переписано (`git filter-branch
  --index-filter "git rm --cached --ignore-unmatch zaminy-tz.md" --prune-empty
  -- --all`, локальний `gc --prune=now`, потім `git push --force`) — коміт-
  хеші після сесії 9 змінились. `git-filter-repo` в середовищі не було
  встановлено, використали вбудований (застарілий, але робочий для разового
  прибирання) `git filter-branch`. Файла більше немає ні в репозиторії, ні
  на диску — я не встигла зберегти повний вміст до видалення, тож посилання
  на нього прибрано і з цього файлу (було на самому початку й у розділі
  "Що лишилось").

**Заплановано на майбутнє (ще не почато):** можливість редагувати профілі
вчителів — і вручну (новий екран/форма, розділ 10 ТЗ цього explicitly не
описував), і через імпорт (`importSchedule.ts`/`ImportScreen.tsx` зараз
тільки додають нових і зливають розклад, повного редагування наявного
вчителя вручну в UI нема). Kate позначила це як "нам в майбутньому
потрібно буде" — не запит на цю сесію, окрема майбутня ітерація.

## Рішення, уточнені під час сесії 1 (в ТЗ дослівно не було)

- Тир визначається трьома ознаками — куратор / викладає клас / присутній
  (`src/ranking/rank.ts`, `classifyTier`). Погодинність (`Teacher.isHourly`) — лише
  позначка в профілі для довідки, на тир не впливає. Якщо погодинник фактично доступний
  до/після власного уроку — це вручну вказаний звичайний блок `presence`, без спецлогіки.
- Бонуси (золота година, 3+ відмов поспіль) підіймають кандидата всередині БУДЬ-ЯКОГО
  тиру, не тільки тиру 1.
- Кандидат, який сам має урок у цьому слоті, виключається зі списку повністю —
  незалежно від того, під який тир він би інакше підійшов.
- Позначка про авдиторію — властивість слоту (однакова для всіх кандидатів у списку),
  а не властивість конкретного кандидата.
- "Уроків сьогодні" на картці — тільки базовий розклад цього дня тижня, без нашарування
  тижневих замін (спрощення, тижневий шар ще не реалізований).
- Мовчання (`silent`) під час підрахунку "відмов поспіль" просто пропускається, а не
  переривається — воно не має рахуватись як відмова.

## Рішення, уточнені під час сесії 2 (в ТЗ дослівно не було)

- `closedVia` при згоді визначається статусом заміни в момент кліку, а не окремим
  вибором каналу: `"chat"` для звичайного опитування, `"voluntary"` якщо заміна вже була
  в статусі `in-chat` (`src/data/actions.ts`, `recordAttempt`).
- Статус "Тупик" пишеться без причини (`взяла сама` / `урок скасовано` / `класи
  об'єднано` з розділу 3 ТЗ ніде не зберігається) — свідоме спрощення, за 3 роки цей
  статус траплявся вкрай рідко.
- Розсилка завчасних замін у чат — **одне спільне повідомлення** на всі поточні
  завчасні заміни зі статусом `open` (розділ 1/5: "списком"), а не окреме на кожну.
  Кнопка "відкрити чат" ховається, доки `GROUP_CHAT_INVITE_URL` в `settings.ts`
  порожній — реального посилання групи ще немає.
- Статистика на профілі (заміни/відмови/мовчання) — за **поточний календарний місяць**
  від реальної дати пристрою, так само як лічильник "замін за місяць" у ранжуванні.
  Наслідок для тестування: сідові дані датовані вереснем 2026, а реальна дата в момент
  розробки — серпень 2026, тож на живому екрані профілю це очікувано покаже нулі.
- Текст WhatsApp-повідомлення (розділ 7) — фіналізований дослівно Kate:
  `"Привіт! Потрібна заміна: {день тижня}, {дата}, урок {N} ({початок}–{кінець}), {клас}
  клас. Підкажи, будь ласка, чи зможеш ти її взяти?"` — без згадки предмета.

## Рішення, уточнені під час сесії 3 (в ТЗ дослівно не було)

- **Формат календаря — список на день, не тижнева сітка.** Kate обрала явно
  (простіше на мобільному, узгоджується з тим, що решта екранів теж працюють з
  однією датою за раз). Тижнева сітка лишається можливим майбутнім розширенням.
- **Без фільтра — порожній стан, а не повний розклад дня.** Kate обрала явно:
  вимагати вибір вчителя/класу/авдиторії, а не рендерити весь шкільний розклад
  одразу.
- Золоті години на календарі **не показуються** — тільки блоки присутності
  (розділ 5 ТЗ прямо каже лише про присутність). Свідоме звуження скоупу, щоб не
  дублювати профіль вчителя.
- Текстова позначка "Заміна — N клас" у профілі замісника (розділ 5 ТЗ) окремо
  не робили — той самий сенс уже покривається `isSubstitution`/`substituteFor` на
  картці слоту в `DayAgenda`, окремого профільного відображення не додавали.

## Рішення, уточнені під час сесії 4 (в ТЗ дослівно не було)

- **Місяць на статистиці можна перемикати** (◀/▶, дефолт — поточний) — на
  відміну від профілю, де місяць жорстко "поточний". Причина: сідові дані
  датовані вереснем 2026, а розробка йде в серпні — без перемикання екран під
  час тестування завжди порожній. Kate обрала явно.
- **Тупики — список із деталями** (дата, клас, урок, хто був відсутній), а не
  лічильник. Kate обрала явно: тупики не прив'язані до конкретного вчителя
  (немає кандидата, що взяв заміну), тож самого числа замало для
  ретроспективи.
- Таблиця по вчителях показує **всіх**, включно з нульовими рядками — це
  зведення для огляду, а не фільтр активності.

## Рішення, уточнені під час сесії 5 (в ТЗ дослівно не було)

- **Сценарій "на весь день" зробили одразу**, а не відклали — Kate обрала
  явно, замість спершу закрити тільки одиночний розбір.
- **Режим (термінова/завчасна) — перемикач у формі з розумним дефолтом**
  (дата = сьогодні → термінова, інша дата → завчасна), а не завжди
  термінова. Kate обрала явно.
- **Мінімальна довжина токена для збігу імені — 3 символи.** Без цього
  прийменники на кшталт "на"/"у" здатні випадково префіксно збігтися з
  коротким ім'ям (наприклад "на" проти "Наталія"). Не з ТЗ — власне
  спрощення, щоб уникнути хибних збігів.
- **Чергування "втрачених" голосних у прізвищах (Кравець → Кравця) і
  чергування приголосних (урок → на уроці) не покриваються** — той самий
  рівень спрощення, що ТЗ явно приймає для звичайних відмінкових закінчень
  ("відмінки міняють закінчення, а не корінь"). Відомий, задокументований
  у коді пробіл, не помилка.
- **Дні тижня для розбору дати — невелика ручна таблиця форм** (називний +
  типовий відмінок після "на"/"у"/"в"), а не загальний відмінювач — 7 днів,
  узагальнення того не вартує.
- **Конфлікт слоту (уже є заміна) блокує створення повністю**, а не просто
  попереджає — Kate ще не просила можливість свідомо перекрити наявну
  заміну, це можна додати пізніше, якщо знадобиться.

## Рішення, уточнені під час сесії 6 (в ТЗ дослівно не було)

- **Групування шести тирів у три кольорові смуги за парами пріоритету
  (1-2/3-4/5-6)**, а не по одному кольору на тир і не по три перших/останні —
  бо палітра Kate дає рівно три рівні акценту, а не шість. Якщо Kate захоче
  інший розподіл, це одна умова (`band` у `TierSection.tsx`).
- **Бейдж "золота година" додали тільки для бонусу від золотої години, не для
  бонусу "3+ відмов поспіль"** — для другого немає усталеної короткої назви
  ніде в застосунку чи документації, а вигадувати нову формулу самостійно
  здалось ризикованим; той бонус і далі видно через підсвічений колір картки
  й число у рядку "відмов поспіль".
- **Семантичні good/warn кольори (згода/відмова) не перефарбовані під
  teal/purple/bronze** — навмисний виняток із загальної палітри, бо швидкість
  розпізнавання кнопки під час дзвінка вчителю важливіша за одноманітність
  бренду.

## Рішення, уточнені під час сесії 7 (в ТЗ дослівно не було)

- **Невідомий `teacherId` у `schedule` — попередження, а не блокування
  імпорту.** ТЗ не описує звірку `schedule` проти списку вчителів взагалі;
  додано самостійно як допомогу проти одруків (Kate вручну збирає JSON із
  HTML/скріншотів через окрему розмову з Claude), але оскільки це не
  порушення формату як такого, а радше евристика, вирішили не блокувати
  імпорт — раптом такий запис справді потрібен (наприклад, вчитель буде
  доданий іншим імпортом пізніше).
- **Прев'ю "що зміниться" перед підтвердженням, а не мовчазний імпорт.**
  Імпорт частково перезаписує базу (мерджить вчителів і розклад) — за
  аналогією з "Тупик" (єдина інша дія в застосунку, що явно підтверджується
  `window.confirm`) підтвердження показує короткий підсумок замість голого
  "Ви впевнені?".
- **Вкладка "Імпорт" — п'ята, а не вбудована в іншу вкладку.** Розглядали
  панель усередині "Активних замін" (за аналогією з `ParseRequestPanel`), але
  імпорт — одноразова процедура на семестр, не щоденна дія, тож окрема
  вкладка не захаращує основний екран.

## Рішення, уточнені під час сесії 8 (в ТЗ дослівно не було)

- **Google Drive замість GitHub як бекенд синхронізації.** ТЗ й попередні
  сесії фіксували задачу як "GitHub-синхронізація", але єдиний робочий
  референс, який надала Kate (застосунок "Стайня"), реалізує Google Drive.
  Kate підтвердила напряму: приймаємо Drive, формулювання в документації
  виправлено заднім числом.
- **Весь стан цілком, виграє свіжіший, без по-записового злиття** — на
  відміну від референсу (`mergeData` по колекціях + tombstone-мапа видалень
  у Стайні). Виправдано тим, що Kate підтвердила: синхронізація потрібна для
  одного акаунта між власними пристроями, а не для кількох різних людей, що
  пишуть одночасно — ризик зіткнення двох записів у вікні кількох секунд
  малий, і ціна складнішої логіки (тег видалень, що чиститься через 90 днів)
  не виправдана. Якщо Kate почне помічати втрачені зміни при паралельній
  роботі на двох пристроях — перехід на по-записове злиття — окрема наступна
  ітерація.
- **`AppState.meta.updatedAt` — нове окреме поле, не перевикористання
  наявного `AppState.updatedAt`.** Останнє вже означає дату збірки бази з
  імпорту (контракт розділу 8 ТЗ, валідується форматом `YYYY-MM-DD`,
  показується користувачу в `ImportScreen`) — використати його для мітки
  синхронізації означало б зламати цей показ і валідацію.
- **`useAppState()` підняли в спільний React-контекст
  (`AppStateContext.tsx`)**, хоча про це прямо не просили. До сесії 8 п'ять
  екранів викликали хук незалежно, і це працювало лише тому, що вони ніколи
  не монтувалися одночасно (умовний рендер вкладок без роутера). Синхронізація
  ж потребує єдиного місця, що веде дебаунс-запис і бейдж статусу для всього
  застосунку одразу — без підняття стану в контекст довелося б або дублювати
  цикл синхронізації 5 разів, або взагалі не мати спільного бейджа.

## Стан проєкту після сесії 11 (2026-08-09)

Останній конкретний пункт із "заплановано на майбутнє" (сесія 10) — ручне
редагування й видалення профілю вчителя на вкладці "Профілі" (раніше зміна
даних вчителя була можлива тільки повним повторним імпортом JSON):

- **`updateTeacher(state, teacherId, patch)`** (`src/data/actions.ts`) —
  той самий патерн, що й інші дії. `patch: Omit<Partial<Teacher>, "id">` —
  типи забороняють міняти `id`. `{ ...t, ...patch }` (просте присвоєння, а
  не `incoming.X ?? existing.X`, як у `mergeTeacher` з `importSchedule.ts`)
  дозволяє явно очистити необов'язкове поле через `phone: undefined` —
  `importSchedule.ts` не чіпали, це окрема від імпорту поведінка.
- **`deleteTeacher(state, teacherId)`** (`src/data/actions.ts`) — повне
  очищення (рішення Kate, див. нижче): прибирає вчителя, його розклад,
  усі заміни, де він відсутній АБО був замісником, і `attempts`, де він
  фігурує сам чи що належали вже видаленим замінам.
- **`TimeBlockListEditor.tsx`** (`src/components/`) — новий переюзаний
  редактор списку `TimeBlock` (день тижня + час від + час до, додати/
  видалити рядок), спільний для `presence` і `goldenHours`. Експортує
  `isTimeBlockValid`, яку перевикористовує форма для валідації.
- **`TeacherEditForm.tsx`** (`src/components/`) — форма редагування всіх
  полів `Teacher`, крім `id`. Верстка на наявних класах `.parse-panel*`
  (та сама конвенція, що й `ParseRequestPanel`/`ImportScreen`), без нових
  паралельних класів форми.
- **`ProfileScreen.tsx`** — кнопки "Редагувати"/"Видалити вчителя" над
  карткою; редагування підміняє картку формою на місці (без переходу на
  інший екран); видалення питає `window.confirm` (той самий патерн, що й
  "Тупик"/імпорт), після підтвердження вибір переходить на наступного
  вчителя за алфавітом.
- Перевірено в Browser pane: форма підвантажує поточні дані вчителя,
  збереження телефону й очищення телефону через порожнє поле (підтверджено
  в localStorage — ключ `phone` реально зникає, а не стає порожнім рядком),
  видалення вчителя з відкритою заміною (`sub-2`, Клименко Оксана) прибирає
  цю заміну і з "Профілів", і з черги на "Активних замінах". `window.confirm`
  у вбудованому прев'ю-браузері не клікається напряму (те саме обмеження,
  що й з Google-попапом у сесії 9) — підтвердження першого кліку (нічого не
  видалилось без "так") перевірили, а сам "так" — тимчасовим оверайдом
  `window.confirm` через `javascript_tool` для діагностики, не через зміну
  коду. 98 юніт-тестів (включно з новими `describe("updateTeacher")` і
  `describe("deleteTeacher")`) і `tsc -b` — зелені.

Створення нового вчителя вручну (не через імпорт) — свідомо поза межами
цієї сесії, не запитувалось.

## Рішення, уточнені під час сесії 11 (в ТЗ дослівно не було)

- **Видалення вчителя — "повне очищення", а не лишити історію/тупики.**
  Kate обрала явно (`AskUserQuestion`, три варіанти): разом із вчителем
  видаляються не тільки розклад, а й усі заміни/спроби, де він фігурує в
  будь-якій ролі (відсутній, замісник, учасник обдзвону) — статистика за
  минулі місяці, де він брав участь, зміниться заднім числом. Свідомо
  простіше за проміжний варіант "позначити тупиком" — Kate не бачила
  цінності лишати сліди вчителя, якого видалили.
- **Немає кнопки "додати нового вчителя вручну"** — і генерація нового,
  унікального в межах бази `id`, і решта UI для порожньої форми "з нуля" —
  свідомо не додавали. Створення й так покриває імпорт JSON; Kate просила
  саме редагування наявних і видалення, не створення.
- **Кнопка "Скасувати" в формі редагування без `window.confirm`** — на
  відміну від видалення. Редагування профілю оборотне (форма завжди
  відкривається знову з поточними даними з бази), а видалення — ні, тому
  підтвердження лише для видалення, за тим самим принципом, що й "Тупик".

## Стан проєкту після сесії 12 (2026-09-01)

Нова тема поза розділом 10 ТЗ — **спарені (об'єднані) класи**: один урок, на
якому сидять два чи більше класів разом. До цієї сесії мітка класу була
непрозорим рядком, і запис із міткою "5-6" не зараховувався ні як урок 5-го,
ні як урок 6-го класу — вчитель такого уроку падав у нижні тири, а сам урок не
з'являвся в календарі жодного з класів.

- **`src/ranking/classes.ts`** (новий модуль + `classes.test.ts`) —
  `splitClassLabel` розкладає мітку на складові класи, `classesOverlap`
  перевіряє спільний клас двох міток. Розбір консервативний, бо звичайна мітка
  теж має дефіс: `/` розділяє завжди, а `-` — **тільки коли всі частини
  числові** (`"5-6"` → `["5","6"]`, `"9-А"` лишається одним класом).
- **`rank.ts`** — `teachesClass` тепер через `classesOverlap`: вчитель
  спареного уроку вважається таким, що викладає в кожному зі своїх класів
  (тир 1/2/3 замість 4/5/6). Пошук запису відсутнього (звідки беруться
  предмет/авдиторія для позначки про лабораторію) — теж через перетин, тож
  знаходить спарений запис, навіть якщо заміна заведена на "5".
- **`effectiveDay.ts`** — заміна, заведена на один клас пари, накриває весь
  спарений слот у календарі.
- **`CalendarScreen.tsx`** — у фільтрі "клас" спарений урок дає окремі пункти
  "5" і "6" (`flatMap(splitClassLabel)`) і показується в розкладі кожного.
- **`resolveRequest.ts`** — перевірка конфлікту "уже є заміна на цей слот"
  ловить перетин зі спареним класом.
- 111 юніт-тестів і `npx tsc -b` — зелені. У браузері не перевіряли: у
  сідових даних спарених класів немає, жива перевірка вимагала б підмішування
  даних, що вже роблять тести.

**Відомий відкритий пункт розділу 9** (не чіпати передчасно) — той самий, що й
з сесії 9: авдиторії насправді дві (Hardlab і Softlab) зі своїми переліками
предметів, а не одна, як зараз у `TECHNICAL_SUBJECT_ROOMS`.

## Рішення, уточнені під час сесії 12 (в ТЗ дослівно не було)

- **"Спарені" = об'єднані класи в одному уроці, а не два уроки підряд.**
  Kate обрала явно (`AskUserQuestion`) — формулювання "спарені уроки" в
  українській школі частіше означає здвоєний урок, тож розбіжність варто
  було зняти до коду. Сценарій "один запит на два уроки підряд" не робили.
- **Приймаємо обидва розділювачі — `"5-6"` і `"9-А/9-Б"`.** Kate обрала явно.
  Звідси й асиметрія правил: дефіс розбирається лише в суто числовій мітці
  (інакше `"9-А"` розпалось би на "9" і "А"), слеш — завжди, тож пари класів
  з літерами записуються через слеш.
- **Навантаження в профілі (`classLoad`) навмисно НЕ розбиває спарену мітку** —
  рядок лишається "5-6 клас". Для вчителя це фізично один урок на тиждень, а
  не два; розбиття подвоїло б лічильник `lessonsPerWeek`.
- **Мітка класу лишається рядком, окремого типу/списку класів не заводили** —
  розбір локальний і чистий (`classes.ts`), формат імпорту (розділ 8 ТЗ) і
  `ScheduleEntry.class` не змінились, тож наявні бази й JSON-імпорти
  працюють без міграції.

## Стан проєкту після сесії 13 (2026-09-01)

Продовження теми сесії 12 — сесія 12 навчила **логіку** розпізнавати спарені
мітки, але **вводити** розклад руками ніде не було (лише імпорт JSON;
`TeacherEditForm` із сесії 11 чіпає поля `Teacher`, не розклад). Додано ручне
редагування базового тижневого розкладу:

- **`setTeacherSchedule(state, teacherId, entries)`** (`src/data/actions.ts`) —
  той самий стиль, що й `updateTeacher`. Замінює **всі** записи розкладу цього
  вчителя на передані (форма веде повний список його уроків). Унікальність
  `(weekday, lesson)` — інваріант `scheduleKey` з `importSchedule.ts` — гарантує
  форма, дія її не перевіряє. Дзвінки/заміни/спроби/чужий розклад не чіпає.
- **`src/components/ScheduleListEditor.tsx`** — переюзаний редактор списку рядків
  розкладу (день тижня + номер уроку + клас(и) + предмет + авдиторія, додати/
  видалити рядок). Експортує `entryToRow`/`rowToEntry`/`emptyRow`/
  `isScheduleRowValid`. Спареність зроблена **явною**: чекбокс "спарений урок" +
  список полів класів, які склеюються через `/` при збереженні. Предмет/
  авдиторія — `<input list=…>` з підказками наявних значень.
- **`src/screens/ScheduleScreen.tsx`** — нова вкладка. `TeacherPicker` зверху +
  форма (`key={teacher.id}` — скидання чернетки при зміні вчителя). Валідація
  вимикає "Зберегти": день ∈ Пн–Пт, урок — ціле ≥ 1, ≥1 непорожній клас (≥2 для
  спареного), предмет непорожній, **нема двох рядків з однаковими
  `(weekday, lesson)`** (повідомлення називає збіг). "Скинути зміни" повертає до
  збереженого стану. `window.confirm` немає — редагування оборотне (як
  "Скасувати" в `TeacherEditForm`, рішення сесії 11).
- **`src/App.tsx`** — вкладка "Розклад" **шоста**, між "Статистика" і "Імпорт"
  (розділ 5 ТЗ фіксує лише перші чотири; "Розклад" — обслуговування бази, поряд
  з "Імпорт", Імпорт лишається останнім).
- Перевірено в Browser pane: вкладка "Розклад" → Ткаченко Ігор → на рядку
  чт/урок 1 ("9-Б") ввімкнено "спарений", додано "9-А", збережено → у
  localStorage `class` став `"9-Б/9-А"`; на "Календарі" фільтр "клас" тепер дає
  окремі пункти "9-А" і "9-Б"; перевірка дублю `(weekday, lesson)` блокує
  "Зберегти" з поясненням. 115 юніт-тестів (нові `describe("setTeacherSchedule")`)
  і `npx tsc -b` — зелені.

**Відомий відкритий пункт розділу 9** (не чіпати передчасно) — той самий:
Hardlab/Softlab у `TECHNICAL_SUBJECT_ROOMS`.

## Рішення, уточнені під час сесії 13 (в ТЗ дослівно не було)

- **Окрема вкладка "Розклад", не всередині "Профілів".** Kate обрала явно
  (`AskUserQuestion`): не кнопка на "Профілях" і не блок у формі "Редагувати", а
  повноцінна вкладка з власним пікером вчителя.
- **Спарені класи — явний перемикач "спарений" + список полів класів, не
  вільний текст із роздільником.** Kate обрала явно. Менше шансів на друкарську
  помилку в роздільнику; застосунок сам склеює через `/`.
- **Канонічний роздільник збереження — `/`.** Наслідок round-trip: числова мітка
  `"5-6"` (з імпорту) при редагуванні вантажиться як пара і зберігається назад як
  `"5/6"`. `classesOverlap` бачить їх однаково, календар/ранжування не зачеплені —
  розбіжність прийнятна, `/` узгоджується з вибором перемикача.
- **`setTeacherSchedule` замінює весь розклад вчителя цілком**, а не по-рядково —
  той самий принцип, що "весь стан, виграє свіжіший" (сесія 8) і просте
  присвоєння в `updateTeacher` (сесія 11). Форма й так тримає повний список.
- **`room` дозволено порожній** у формі (зберігається як `""`) — на відміну від
  предмета; реальні дані інколи без авдиторії, а тип `ScheduleEntry.room`
  лишається `string`.
- **Створення вчителя з нуля тут теж не робили** — як і в сесії 11, лишається за
  імпортом JSON. Редагувати можна лише розклад наявного вчителя.

## Стан проєкту після сесії 14 (2026-09-01)

Kate поскаржилась, що вкладка "Розклад" (сесія 13) незручна, надто для
молодшої ланки з власними номерами уроків. З'ясувалось у діалозі: справжня
потреба — не повний тижневий графік, а можливість просто **позначити, у яких
класах викладає вчитель** (пара клас + предмет). До цієї сесії "викладає клас"
було вивідним лише з `state.schedule` — щоб застосунок це знав, треба було
завести кожен урок за тиждень із номером; а номер уроку для молодшої ланки —
штучна "внутрішня нумерація" (єдиний масив `bells` на всю школу).

- **`Teacher.teaches?: TeachingAssignment[]`** (`src/types/teacher.ts`) — нове
  необов'язкове поле, `{ subject: string; classes: string[] }[]`. Мітки класів —
  ті самі рядки, що й у розкладі (звичайні "9-А", спарені "5-6"/"5/6").
- **`rank.ts`** — `teachesClass` → `teacherTeachesClass(teacher, schedule, class)`:
  тир береться з `teacher.teaches` **об'єднано (union)** з класами, вивіденими зі
  `schedule`. Бази без нового поля працюють як раніше. Предмет відсутнього для
  позначки "лабораторія" (розділ 9) теж падає назад на `absentTeacher.teaches`,
  якщо запису розкладу на цей слот немає (`absentSubject`).
- **`src/components/TeachingAssignmentsEditor.tsx`** — новий редактор: блок на
  предмет (`<input list>` з підказками) + список полів класів ("+ предмет",
  "+ клас", ✕). Верстка перевикористовує наявні класи `.schedule-editor__*`.
  Експортує `isAssignmentValid` / `cleanAssignments` (trim + викидання порожніх).
- **`TeacherEditForm.tsx`** — блок "Викладає (предмет → класи)" між "Предмети" і
  "Присутність". Порожній список зберігається як `teaches: undefined` (той самий
  патерн очищення необов'язкового поля, що `phone` у сесії 11). `updateTeacher`
  не чіпав — патч `Omit<Partial<Teacher>, "id">` приймає `teaches` сам.
- **`ProfileCard.tsx`** — секція "Викладає" на картці (список "предмет: класи"
  або підказка "заповнюється в «Редагувати»").
- **`src/data/seed.ts`** — `teaches` у 4 вчителів (Ткаченко, Клименко, Гончарук,
  Коваль), дзеркалить їхній наявний розклад, тож union нічого не змінює й старі
  тести ранжування лишаються стабільними.
- 117 юніт-тестів (нові `describe("явне поле teaches")` у `rank.test.ts`) і
  `npx tsc -b` — зелені. У браузері не перевіряли: порт 5173 зайнятий
  дев-сервером іншого чату (те саме обмеження, що в сесіях 11/13).

Вкладка "Розклад" із повним тижневим графіком лишилась незмінною — тепер це
необов'язкова деталізація (потрібна лише "Календарю", яким Kate майже не
користується).

**Відомий відкритий пункт розділу 9** (не чіпати передчасно) — той самий:
Hardlab/Softlab у `TECHNICAL_SUBJECT_ROOMS`.

## Рішення, уточнені під час сесії 14 (в ТЗ дослівно не було)

- **Справжня потреба — "класи, у яких викладає вчитель", а не зручніша форма
  вводу повного розкладу.** Kate уточнила це посеред обговорення
  (`AskUserQuestion` про модель дзвінків виявився не тим напрямком). Повний
  тижневий графік лишається, але перестає бути єдиним джерелом для визначення
  тиру.
- **Форма поля — групування по предмету** (`{ subject, classes[] }`), а не плоскі
  рядки "клас + предмет". Kate сказала "роби як зручніше" — вчитель одного
  предмета заповнює один блок і додає чипи класів, без повторення предмета.
- **`teaches` об'єднується з розкладом, не заміщає його.** Хто вже має повний
  розклад — нічого не втрачає; хто заповнив лише `teaches` — отримує коректний
  тир. Свідомі послаблення без розкладу (Kate знає й прийняла): "уроків сьогодні"
  = 0 (дрібний тайбрейк сортування), кандидата з власним уроком у слот не
  сховає авто, зайнятість кабінету-лабораторії не перевіряється, "Календар" для
  такого вчителя порожній.
- **Редагування `teaches` — у формі "Редагувати вчителя" на "Профілях", не окрема
  вкладка.** На відміну від сесії 13 (там Kate явно хотіла окрему вкладку для
  розкладу) — тут це кілька міток на вчителя, органічно лягає у вже наявну форму.
- **Дзвінки старша/молодша й `teaches` в JSON-імпорті — не робили.** Kate має
  реальні вересневі часи обох дзвінків, але після уточнення потреби окремі
  розклади дзвінків стали непотрібні для головного сценарію; імпорт `teaches` —
  можливий наступний крок.
- **Створення вчителя з нуля — знову поза межами** (як сесії 11, 13).

## Стан проєкту після сесії 15 (2026-09-01)

Kate поставила задачу: оновлювати розклади через імпорт JSON так, щоб не
знищити профільну інфу, яку вона заповнила руками. Розбір показав рівно одну
справжню прогалину — `teaches` (сесія 14) додали в `Teacher` пізніше за код
імпорту, тож `mergeTeacher` (`src/data/importSchedule.ts`) його взагалі не
переносив: будь-який вчитель у масиві `teachers` імпорту втрачав `teaches`,
навіть якщо JSON збирався тільки заради розкладу. Решта необов'язкових полів
(`phone`, `curatorOf`, `isHourly`, `alwaysPresent`, `presence`, `goldenHours`)
уже мали fallback `?? existing` і зберігались коректно.

- **`ImportTeacher.teaches?: TeachingAssignment[]`** (`src/types/importFormat.ts`) —
  нове необов'язкове поле формату імпорту.
- **`mergeTeacher`** (`src/data/importSchedule.ts`) — додано
  `teaches: incoming.teaches ?? existing?.teaches` (той самий патерн, що й інші
  необов'язкові поля; `undefined`, якщо нема з жодного боку — так само, як
  `updateTeacher` очищає це поле).
- **`checkTeacher`** (`src/data/importValidation.ts`) — коли `teaches` присутнє,
  валідується як масив `{ subject: string, classes: string[] }` з точним шляхом
  помилки (`teachers[0].teaches[0].classes: очікувався масив рядків`). Відсутнє —
  пропускається, як `presence`/`goldenHours`.
- **`src/data/importSchedule.test.ts`** — новий файл: вчитель в імпорті без
  `teaches` зберігає наявне; з `teaches` — заміщує; `phone`/`presence`/
  `goldenHours` зберігаються; імпорт лише розкладу (`teachers: []`) не змінює
  запис вчителя жодним полем; заміни/спроби не чіпаються. Плюс кейси `teaches`
  у `importValidation.test.ts`. 125 юніт-тестів і `npx tsc -b` — зелені.
- **Рекомендований робочий процес для Kate** (не код, а домовленість): щоб
  оновити лише розклади наявних вчителів — JSON із `teachers: []` і лише
  потрібними записами `schedule`; тоді жодне поле вчителя не чіпається й
  попередження про "невідомий id" не з'являється (бо id є в наявній базі).
  Повний JSON із блоком `teachers` тепер теж безпечний для `teaches`.

**Свідомо не робили (Kate обрала "тільки teaches", `AskUserQuestion`):**
- `subjects` і далі **завжди** перезаписується з JSON (обов'язкове поле формату,
  без fallback) — не чіпали.
- Прев'ю "Що зміниться" в `ImportScreen` і далі показує лише лічильники, не
  перелік профільних полів, які буде перезаписано.
- Імпорт розкладу і далі тільки додає/замінює записи за ключем
  `(teacherId, weekday, lesson)`, **не видаляє** застарілі — для повної заміни
  розкладу вчителя лишається вкладка "Розклад" (`setTeacherSchedule`, сесія 13).

**Відомий відкритий пункт розділу 9** (не чіпати передчасно) — той самий:
Hardlab/Softlab у `TECHNICAL_SUBJECT_ROOMS`.

## Рішення, уточнені під час сесії 15 (в ТЗ дослівно не було)

- **`teaches` в імпорті — fallback `?? existing`, не завжди-заміщення.** Той
  самий принцип, що вже діяв для `phone`/`presence`/`goldenHours` із сесії 7:
  Kate часто збирає JSON в окремій розмові з Claude, яка про `teaches` не знає,
  тож "відсутнє поле = лишити наявне" — єдина безпечна поведінка.
- **`subjects` лишили як є (завжди перезапис).** Kate явно обрала мінімальний
  обсяг ("тільки teaches"). Зробити `subjects` необов'язковим із fallback
  розглядали, але тоді імпорт не зміг би прибрати предмет зі списку вчителя, а
  сам список короткий і легко вказати правильно.
- **UI прев'ю не розширювали.** Опція "показувати, які поля перезапише" була на
  столі — Kate відхилила разом із рештою необов'язкового обсягу.
